import type { PaginationParams } from './types';
declare const _default: {
    pageSize: (pagination: PaginationParams | undefined) => string;
    page: (pagination: PaginationParams | undefined) => string;
};
export default _default;
